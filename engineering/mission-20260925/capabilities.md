# Capabilities used, 2026-09-25

No new package, plugin, or MCP server was installed.

Used the existing kiln `.venv` Python, the web `vitest` binary, `sqlite3`, `launchctl`, and `memory_pressure`.

`POST /chat` requires a session because exposure is private. That boundary was left in place.

# A8 KV

现场 9B `/Users/rainhuang/Desktop/models/qwen3.5-9b-hauhau-aggressive-mxfp4/config.json` 的 `text_config.full_attention_interval` 为 **4**。只读了配置文本，没有加载模型。

`127.0.0.1:8081` 的监听进程是 PID **1581**。该 PID 的 `ps` 参数里没有 `kv-bits`。

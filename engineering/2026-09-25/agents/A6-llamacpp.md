# A6 llama.cpp

`which llama-cli llama-server`：两者都不在 PATH 中（`llama-cli not found`，`llama-server not found`）。

`/Users/rainhuang/Desktop/models` 下 `find -maxdepth 3 -iname '*.gguf'` 命中 **0** 个文件。没有编译，没有构建。
